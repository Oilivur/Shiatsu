name: Deploy Vite App to GitHub Pages

on:
  push:
    branches:
      - main  # 🚀 Runs when pushing to main branch

  workflow_dispatch:  # Allows manual runs from GitHub Actions tab

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22  # 🔥 Use Node.js 22 (matches your local version)

      - name: Install Dependencies
        run: npm ci  # Ensures a clean install for CI/CD

      - name: Build Project
        run: npm run build  # Ensures Vite builds the project correctly

      - name: Upload Artifact
        uses: actions/upload-artifact@v4  # ✅ Fixed incorrect action name
        with:
          name: github-pages  # ✅ This name is important for the next step
          path: dist  # ✅ Make sure this matches your build output folder

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1

